/*
* Tasty 1.0
* RestoReady v1.0
* Copyright 2014 MH Communication.
* Author MH Communication @MHCommunication
* Built with Sass - http://sass-lang.com/

* Some things to know about this file:
    - Sass is compiled on RestoReady's server so you don't need to convert it to CSS yourself
    - The output CSS is compressed and comments are removed
    - You cannot use @imports in this file
    - Helpers variables, mixins, and starter classes are provided. Change as needed.
    - The file is prepped with a CSS reset
*/
/* Table of Contents
==================================================
  Colors settings
  Box Model
  Breakpoint and Grid Variables
  General Variables
  Sass Mixins
  Dependency-free breakpoint mixin
  Reset
  Grid Setup
  Basic Styles
  Helper classes
  Typography
  Lists
  Images and Iframes
  Forms
  Links and buttons
  Layout
  Modules
  Icomoon Font
  Breakpoints
================================================== */

// ==============================================================================
// #Colors
// ==============================================================================
$body-background: {{ theme_settings.body-background }};
$body-color: {{ theme_settings.body-color }};
$body-color-second: {{ theme_settings.body-color-second }};
$accent-color: {{ theme_settings.accent-color }};

$slider-title-color: {{ theme_settings.slider-title-color }};
$slider-subtitle-color: {{ theme_settings.slider-subtitle-color }};
$slider-link-color: {{ theme_settings.slider-link-color }};
$slider-link-hover-color: {{ theme_settings.slider-link-hover-color }};

$back-to-menu-link-color: {{ theme_settings.back-to-menu-link-color }};
$back-to-menu-link-hover-color: {{ theme_settings.back-to-menu-link-hover-color }};
$nav-link-color: {{ theme_settings.nav-link-color }};
$ribbon-text-color: {{ theme_settings.ribbon-text-color }};
$menus-title: {{ theme_settings.menus-title }};
$dish-title: {{ theme_settings.dish-title }};
$menus-nav-back: {{ theme_settings.menus-nav-back }};

// Foods
$featured-star-color: {{ theme_settings.featured-star-color }};

// Prices
$price-label-color: {{ theme_settings.price-label-color }};
$price-value-color: {{ theme_settings.price-value-color }};

// Tags
$label-vegetarian-background: {{ theme_settings.label-vegetarian-background }};
$label-vegetarian-color: {{ theme_settings.label-vegetarian-color }};
$label-new-background: {{ theme_settings.label-new-background }};
$label-new-color: {{ theme_settings.label-new-color }};
$label-no-pork-background: {{ theme_settings.label-no-pork-background }};
$label-no-pork-color: {{ theme_settings.label-no-pork-color }};
$label-no-meat-background: {{ theme_settings.label-no-meat-background }};
$label-no-meat-color: {{ theme_settings.label-no-meat-color }};

// Featured menus
$featured-menus-background:   {{ theme_settings.featured-menus-background }};
$featured-menus-title-color:  {{ theme_settings.featured-menus-title-color }};
$featured-menus-border-color: {{ theme_settings.featured-menus-border-color }};

// Featured foods
$featured-foods-background:   {{ theme_settings.featured-foods-background }};
$featured-foods-title-color:  {{ theme_settings.featured-foods-title-color }};
$featured-foods-border-color: {{ theme_settings.featured-foods-border-color }};

//Formulaires
$label-form: {{ theme_settings.label-form }};
$border-form: {{ theme_settings.border-form }};

// ==============================================================================
// #Box model
// ==============================================================================
*, input, :before, :after {
  -webkit-box-sizing:border-box;
  -moz-box-sizing:border-box;
  box-sizing:border-box;
}

// ==============================================================================
// #Breakpoint and Grid Variables
// ==============================================================================
$viewportIncrement: 1px;

$small: 480px;
$medium: 1023px;
$large: 1024px;

$postSmall: $small + $viewportIncrement;
$preMedium: $medium - $viewportIncrement;
$preLarge: $large - $viewportIncrement;

/* The following are dependencies of csswizardry grid */
$breakpoints: (
  'small' '(max-width: #{$small})',
  'medium' '(min-width: #{$postSmall}) and (max-width: #{$medium})',
  'medium-down' '(max-width: #{$medium})',
  'large' '(min-width: #{$large})'
);
$breakpoint-has-widths: ('small', 'medium', 'medium-down', 'large');
$breakpoint-has-push:  ('medium', 'medium-down', 'large');
$breakpoint-has-pull:  ('medium', 'medium-down', 'large');

// ==============================================================================
// #General Variables
// ==============================================================================


/* Width variables */
$siteWidth: 960px;
$gutter: 24px;
$gridGutter: 24px; // can be a %, but nested grids will have smaller margins because of it

/* Radius */
$radius: .25em;

/* Typography */
$headerFontStack: {{ theme_settings.header-font }};
$bodyFontStack: {{ theme_settings.body-font }};



// ==============================================================================
// #Sass Mixins
// ==============================================================================
.clearfix {
  &:after {
    content: "";
    display: table;
    clear: both; }
  *zoom: 1;
}
@mixin clearfix() {
  @extend .clearfix;
}

@mixin box-shadow($shadow) {
  -webkit-box-shadow: #{$shadow};
  -moz-box-shadow:    #{$shadow};
  box-shadow:         #{$shadow};
}

@mixin transition($transition:0.1s all) {
  -webkit-transition: #{$transition};
  -moz-transition: #{$transition};
  -o-transition: #{$transition};
  transition: #{$transition};
}

@mixin gradient($from, $to, $fallback) {
  background: $fallback;
  background: -moz-linear-gradient(top, $from 0%, $to 100%);
  background: -webkit-gradient(linear, left top, left bottom, color-stop(0%,$from), color-stop(100%,$to));
  background: -webkit-linear-gradient(top, $from 0%,$to 100%);
  background: -o-linear-gradient(top, $from 0%,$to 100%);
  background: -ms-linear-gradient(top, $from 0%,$to 100%);
  background: linear-gradient(top bottom, $from 0%,$to 100%);
}

@mixin backface($visibility: hidden) {
  -moz-backface-visibility: $visibility;
  -webkit-backface-visibility: $visibility;
  -o-backface-visibility: $visibility;
  backface-visibility: $visibility;
}



// ==============================================================================
// Dependency-free breakpoint mixin
// http://blog.grayghostvisuals.com/sass/sass-media-query-mixin/
// ==============================================================================
$min: min-width;
$max: max-width;
@mixin at-query ($constraint, $viewport1, $viewport2:null) {
  @if $constraint == $min {
    @media screen and ($min: $viewport1) {
      @content;
    }
  } @else if $constraint == $max {
    @media screen and ($max: $viewport1) {
      @content;
    }
  } @else {
    @media screen and ($min: $viewport1) and ($max: $viewport2) {
      @content;
    }
  }
}

// ==============================================================================
// #Reset
// ==============================================================================
html,body,div,span,applet,object,iframe,h1,h2,h3,h4,h5,h6,p,blockquote,pre,a,abbr,acronym,address,big,cite,code,del,dfn,em,img,ins,kbd,q,s,samp,small,strike,strong,sub,sup,tt,var,b,u,i,center,dl,dt,dd,ol,ul,li,fieldset,form,label,legend,table,caption,tbody,tfoot,thead,tr,th,td,article,aside,canvas,details,embed,figure,figcaption,footer,header,hgroup,menu,nav,output,ruby,section,summary,time,mark,audio,video{border:0;font-size:100%;font:inherit;vertical-align:baseline;margin:0;padding:0;}article,aside,details,figcaption,figure,footer,header,hgroup,menu,nav,section{display:block;}ol,ul{list-style:none;}blockquote,q{quotes:none;}blockquote:before,blockquote:after,q:before,q:after{content:none;}table{border-collapse:collapse;border-spacing:0;}code,pre{background-color:#faf7f5;font-family:Consolas,monospace;border:0 none;padding:0 2px;color:#51ab62;}pre{overflow:auto;padding:0.5em;margin:0 0 1em;}


// ==============================================================================
// #Grid Setup
//
// - Based on csswizardry grid, but with floated columns and a fixed gutter size
// - Breakpoints defined above, under #Breakpoint and Grid Variables
// - Note the inclusion of .grid-uniform to take care of clearfixes on evenly sized grid-items
// ==============================================================================
$responsive:         true;
$mobile-first:       true;
$use-silent-classes: false;
$push:               true;
$pull:               false;

$class-type: unquote(".");

@mixin grid-media-query($media-query) {
  $breakpoint-found: false;

  @each $breakpoint in $breakpoints {
    $name: nth($breakpoint, 1);
    $declaration: nth($breakpoint, 2);

    @if $media-query == $name and $declaration {
      $breakpoint-found: true;

      @media only screen and #{$declaration} {
        @content;
      }
    }
  }

  @if $breakpoint-found == false{
    @warn "Breakpoint '#{$media-query}' does not exist";
  }
}

/*------------------------------------*\
  $HELPERS
\*------------------------------------*/
/**
 * Create helper show/hide classes around our breakpoints.
 */
@mixin device-helper($namespace:"") {

  #{$class-type}#{$namespace}show        { display:block!important; }
  #{$class-type}#{$namespace}hide        { display:none!important; }

  #{$class-type}#{$namespace}text-left   { text-align:left!important; }
  #{$class-type}#{$namespace}text-right  { text-align:right!important; }
  #{$class-type}#{$namespace}text-center { text-align:center!important; }

  #{$class-type}#{$namespace}left        { float:left!important; }
  #{$class-type}#{$namespace}right       { float:right!important; }

}

/* Our regular, non-responsive width and helper classes. */
@include device-helper();

/* Our responsive classes, if we have enabled them. */
@if $responsive == true {

  @each $name in $breakpoint-has-widths {
    @include grid-media-query($name) {
      @include device-helper('#{$name}--');
    }
  }

}

// ==============================================================================
// #Basic Styles
// ==============================================================================
.wrapper {
  @include clearfix();
  max-width: $siteWidth;
  margin: 0 auto;
  padding: 0 $gutter;
}

// ==============================================================================
// #Helper Classes
// ==============================================================================
.left { float: left; }
.right { float: right; }
.text-center { text-align: center; }
.text-right { text-align: right; }
.hidden { display: none; }
.hide-text {
  height: 1px;
  width: 1px;
  position: absolute;
  overflow: hidden;
  top: -10px;
}
.display-table {
  display: table;
}

.display-table-cell {
  display: table-cell;
  vertical-align: middle;
}

// ==============================================================================
// #Typography
// ==============================================================================
body,
input,
textarea,
button,
select {
  font-size: 87.5%; // Henseforth known as 1em (assumed at 14px)
  line-height: 1.6;
  font-family: $bodyFontStack;
  font-weight: 300;
  -webkit-font-smoothing: antialiased;
  -webkit-text-size-adjust: 100%;
}

h1, h2, h3, h4, h5, h6 {
  display: block;
  font-family: $headerFontStack;
  font-weight: normal;
  line-height: 1.4;
  a {
    font-weight: inherit;
    text-decoration: none;
  }
}

// Pixel to EM conversion tool http://pxtoem.com/
h1 { font-size: 3em; }       // 42px
h2 { font-size: 2.429em; }   // 34px
h3 { font-size: 2em; }       // 28px
h4 { font-size: 1.429em; }   // 20px
h5 { font-size: 1.143em; }   // 16px
h6 { font-size: 1em; }       // 14px

p { margin: 0 0 1em 0; }
p img { margin: 0; }

em { font-style: italic; }
strong { font-weight: bold; }
small { font-size: 80%; }
sup {
  position: relative;
  top: -0.5em;
  font-size: 60%;
}

// ==============================================================================
// #Lists
// ==============================================================================
ul, ol { margin-bottom: 1em; }
ul { list-style: none outside; }
ol { list-style: decimal; }
ol, ul.square, ul.disc { margin-left: 1em; }
ul.square { list-style: square outside; }
ul.disc { list-style: disc outside; }
ol.alpha { list-style: lower-alpha outside; }
li { margin-bottom: 0.25em; }
.inline-list li {
  display: inline-block;
  margin-bottom: 0;
}

// ==============================================================================
// #Images and Iframes
// ==============================================================================
img.auto,
.grid-item img,
.grid-item iframe {
  max-width: 100%;
  height: auto;
}

img.circle {
  border-radius: 50%;
}

// ==============================================================================
// #Forms
// ==============================================================================

input,
textarea,
select,
fieldset {
  max-width: 100%;
}

input,
select,
textarea {
  font-size: 1em;
  line-height: 1.42;
  margin: 0;
  padding: .2rem;
}

input {
  display: inline-block;
  width: auto;
}

button,
input[type="submit"] {
  cursor: pointer;
}

/* Input width and border */
input[type="text"],
input[type="search"],
input[type="password"],
input[type="email"],
input[type="file"],
input[type="number"],
input[type="tel"],
textarea,
select {
  display: block;
  max-width: 100%;
  width: 100%;
}

textarea {
  min-height: 100px;
}

input[type="checkbox"],
input[type="radio"] {
  display: inline;
  margin: 0;
}

/* Form labels */
label,
legend {
  display: block;
  margin-bottom: 2px;
}

label[for] {
  cursor: pointer;
}

/* Horizontal Form */
form.form-horizontal,
.form-horizontal {
  margin-bottom: 0;

  input[type="text"],
  input[type="search"],
  input[type="password"],
  input[type="email"],
  input[type="file"],
  input[type="number"],
  input[type="tel"],
  textarea,
  select,
  label {
    display: inline-block;
    margin-bottom: 0;
    width: auto;
  }
}

// ==============================================================================
// #Links and Buttons
// ==============================================================================
.button {
  border: 0;
  color: white;
  cursor: pointer;
  padding: .5em 2em;
  text-decoration: none;
  vertical-align: middle;
  white-space: nowrap;
  -webkit-appearance: none;
  text-align: center;
  margin: 2rem auto;
  display: block;
  width: 250px;
  font-weight: 700;

  &.button--small {
    font-size: 0.75em;
    padding: .25em .5em;
  }

  &.button--full {
    width: 100%;
  }

  &[disabled],
  &.disabled {
    cursor: default;
  }
}
.button-orange {
    color: $accent-color;
    background-color: none;
    border: 2px solid $accent-color;
    @include transition(background-color 0.25s);
    &:hover {
      background-color: $accent-color;
      color: #fff;
    }
}
.button-black {
    color: white;
    background-color: rgba(0,0,0,1);
    border: 2px solid black;
    @include transition(background-color 0.25s);
    &:hover {
      background-color: rgba(0,0,0,0);
      color: black;
    }
}
.button-white {
    color: black;
    background-color: rgba(255,255,255,1);
    border: 2px solid white;
    @include transition(background-color 0.25s);
    &:hover {
      background-color: rgba(255,255,255,0);
      color: white;
    }
}

.button-custom {
    color: white;
    background-color: rgba(255, 164, 46, 1);
    border: 2px solid $accent-color;
    font-weight: 700;
    width: 250px;
    @include transition(background-color 0.25s);
    &:hover {
      background-color: rgba(255, 164, 46, 0);
      color: $accent-color;
    }
}

// ==============================================================================
// #Layout
// ==============================================================================
@font-face {
  font-family: "Intro Inline";
  src: url("{{ 'IntroInline.eot' | asset_url }}");
  src: local("Intro Inline"), url("{{ 'IntroInline.woff' | asset_url }}") format('woff'), url("{{ 'IntroInline.ttf' | asset_url }}") format('truetype'), url("{{ 'IntroInline.svg' | asset_url }}") format('svg');
  font-weight: normal;
  font-style: normal;
}
body {
  color: $body-color;
  background-color: $body-background;
}
a {
  text-decoration: none;
  color: $body-color;
}
.clearer {
  clear: both;
}
.page {
  @extend .clearfix;
}

.main-header {
}
  .logo {
    margin: 1em 1em;
    display: inline-block;
    vertical-align: middle;
  }
    .logo__link {
      text-decoration: none;
      display: block;
      img {
        width: 150px;
        margin: 0 auto;
        display: block;
      }
    }

.main-nav {
  @include clearfix();
  background-color: $body-background;
}
.select-nav {
  width: 200px;
  margin: 1rem auto;
}
.navigation {
  text-align: center;
}

.main-content {
  margin-bottom: 1em;
}
.main-footer {
}
  .menu-footer {
    text-align: center;
    li {
      display: inline-block;
      margin: 0 0.5rem;
      a {
        @include transition(color .25s);
        font-weight: 700;
        color: $nav-link-color;
        &:hover {
          color: $accent-color;
        }
      }
    }
  }
    .social__items {
      margin: .75em 0;
      text-align: center;
    }
      .social__item {
        display: inline-block;
      }
        .social__link {
          @include transition(color .25s);
          color: $body-color;
          display: inline-block;
          vertical-align: middle;
          font-size: 2em;
          padding: .5em;
          text-decoration: none;
          &:hover, &:focus {
            color: $accent-color;
          }
        }
          .social__label {
            @extend .hide-text;
          }

        .social__link--phone,
        .social__link--envelope {
          font-size: 1.5em;
        }

  .footer__legal {
    margin-bottom: 1em;
    text-align: center;
  }
    .legal__link {
      color: $body-color;
      text-decoration: none;
    }
  .select-nav {
    border: 2px solid black;
  }


// Forms
.field-group {
  margin-bottom: 1em;
}
  .field-group__legend {
    font-size: 1.5em;
    font-weight: 700;
    margin-bottom: 1em;
  }

.input,
.select,
.textarea {
  margin: 0 0 1em;
  input {
    border: 1px solid $border-form;
  }
}
.textarea-contact {
  border: 1px solid $border-form;
}

// Messages
.message {
  padding: .5em 1em;
  font-weight: 700;
  i {
    font-size: 2.5rem;
    vertical-align: middle;
  }
}
.message--success {
  background-color: green;
  color: white;
}
.message--notice {
  background-color: $accent-color;
  color: white;
}
.message--error {
  background-color: #c00;
  color: white;
}

.input-hint {
  font-weight: 700;
}
.input-hint--error {
  color: #c00;
}

// ==============================================================================
// #Modules
// ==============================================================================
.page-header {
  padding: 1em 0;
}
  .page-header__header {
  }
    .page-header__title {
      color: $body-color;
      font-family: $bodyFontStack;
      font-size: 2rem;
    }
    .page-header__description {
      color: $body-color;
      font-size: 1.2rem;
      p {
        font-size: 1.2rem;
        margin-bottom: 0;
      }
    }
  .page-body {
    ol, ul, dl {
      margin-left: 2em;
    }
    ul {
      list-style: disc;
    }
    img {
      max-width: 100% !important;
      height: auto !important;
    }
    a {
      color: $accent-color;
      text-decoration: none;
      &:hover, &:focus {
        color: #999;
      }
    }
  }

// Home

.slider {
  position: relative;
  text-align: center;
  margin-bottom: 2rem;
  background-color: #000;
  height: 640px;
}
.slider__text {
  position: absolute;
  top: 30%;
  width: 100%;
  color: white;
}
.slider-title {
  font-family: "Intro Inline";
  color: $slider-title-color;
}
.slider-subtitle {
  font-family: $bodyFontStack;
  font-size: 100%;
  font-weight: 700;
  color: $slider-subtitle-color;
}
.slider-button {
  .slider-link {
    color: $slider-link-color;
  }
  &:hover {
    .slider-link {
      color: $slider-link-hover-color;
    }
  }
}
.menus--featured {
  background-color: $featured-menus-background;
  margin-bottom: 1em;
  padding: 1em 0;
}
  .menus__item--home {
    text-align: center;
    margin-top: 2rem;
  }
  .menu__title--home {
    font-family: $bodyFontStack;
    font-size: 28px !important;
    text-transform: none !important;
  }
  .post-thumbnail {
    position: relative;
    margin: 1rem 0;
  }
  .post-thumbnail-wrapper {
    position: relative;
    margin: 0 auto;
    width: 250px;
    height: 250px;
  }
  .post-thumbnail img {
    border-radius: 50%;
    width: 250px;
  }
  .post-thumbnail-no-image {
    background-color: #bbb;
    border-radius: 50%;
    width: 250px;
    height: 250px;
  }
  .menu__link-home {
    position: absolute;
    top: 0;
    width: 250px;
    height: 250px;
    border-radius: 50%;
    background-color: none;
    opacity: 0.8;
    @include transition(background-color .15s);
    &:hover {
      background-color: $body-color;
    }
    &:hover .post-find-more {
      display: block;
    }
  }
  .post-find-more {
    width: 250px;
    height: 250px;
    border-radius: 50%;
    color: $body-color-second;
    display: none;
    font-weight: 700;
    span {
      padding-top: 45%;
      display: block;
      .divider {
        border-top: 1px solid $body-color-second;
        border-bottom: 1px solid $body-color-second;
        width: 110px;
        margin: 0 auto;
      }
    }
  }
  .menu__description--home {
    color: #333;
    font-size: 14px;
  }
  .link-view-menu {
    font-size: 14px;
    font-weight: 700;
    color: #333;
    text-transform: uppercase;
    @include transition(color .25s);
    &:hover {
      color: $accent-color;
    }
  }

  .menus__children {
    list-style: none;
    margin-left: 0rem;
  }
    .menus__child {
      font-size: 1.2em;
      text-align: center;
      margin-bottom: 2rem;
    }
      .menus__child-link {
        color: black;
        text-decoration: none;
        &:hover, &:focus {
          color: #333333;
        }
      }
.foods--featured {
  background-color: $featured-foods-background;
  margin-bottom: 1em;
  padding: 1em 0;
  .section__title {
    color: $featured-foods-title-color;
    border-bottom: 1px solid $featured-foods-border-color;
    margin-bottom: .75em;
  }
}

.parallax-section {
  {% if theme_settings.home-display-about-background %}
    background: url({{ 'home-about-background.jpg' | asset_url }});
  {% endif %}
  background-color: #000;
  background-attachment: fixed !important;
  background-repeat: no-repeat;
  background-size: cover !important;
  background-position: center center;
  text-align: center;
  overflow: hidden;
  margin: 50px 0;
}
.parallax-section-wrapper {
  padding: 50px 0;
  background-color: rgba(0,0,0,0.5);
}
.parallax-section .text {
  color: white;
  font-size: 16px;
  line-height: 30px;
  margin-top: 56px;
  font-weight: 700;
}
.parallax-section .button {
  margin-top: 57px;
}
.parallax-section .title {
  font-size: 52px;
  font-family: $headerFontStack;
  color: #fff;
  text-align: center;
}
.parallax-section .title-before {
  color: #fff;
  text-transform: uppercase;
  font-size: 15px;
  font-weight: bold;
  line-height: 50px;
  text-align: center;
  font-family: $bodyFontStack;
}

.opening-hours {
  margin-bottom: 3em;
  text-align: center;
  .section__title {
    margin-bottom: 0.25em;
    font-family: $headerFontStack;
  }
  dl {
    margin-bottom: 0.5rem;
  }
  dt {
    font-size: 14px;
    font-weight: 700;
    text-transform: uppercase;
  }
  dd {
    font-size: 100%;
    margin-bottom: 12px;
    line-height: 1.2;
  }
}
  .opening-days {
    @extend .clearfix;
    margin-bottom: 1em;
  }
    .opening-day {
      clear: left;
      float: left;
      font-size: 1.2em;
      width: 40%;
    }
    .opening-shifts {
      font-size: 1.2em;
      margin-left: 50%;
    }
  .opening-hours__information {
    font-size: 1.1em;
    margin-bottom: 1em;
  }

.directions {
  min-height: 280px;
}
.directions, .business-view {
  margin-bottom: 1em;
  text-align: center;
  position: relative;
}
.localisation-title {
  font-family: $headerFontStack;
  font-size: 3rem;
}
  .wrapp-see-location {
    background: $accent-color;
    background-color: transparentize($accent-color, 0.3);
    position: absolute;
    top: 0;
    bottom: 0;
    width: 100%;
    min-height: 280px;
    padding: 30px 0;
    h2 {
      font-family: $bodyFontStack;
      font-size: 100%;
    }
  }
    .map__image {
      @extend .auto;
    }
    .map__image,
    .map iframe {
      vertical-align: middle;
    }
  .address {
    color: black;
    font-size: 1.2em;
  }

//menus

.menu {
  padding-bottom: 1.5em;
}

  .menu__image-container {
    margin-bottom: 1em;
  }
    .menu__image {
      @extend .auto;
    }
    .menu__title {
      color: #333333;
      margin-bottom: .5em;
      text-align: center;
      font-family: $bodyFontStack;
    }
    .menu--featured {
      .icon-star {
        color: $featured-star-color;
      }
    }
  .menu__description {
    color: black;
    font-size: 1.2em;
    margin-bottom: 1em;
    text-align: center;
  }
  .back-menu-slider {
    background-color: #555;
    text-align: center;
    background-repeat: no-repeat;
    background-size: cover;
    margin-bottom: 1rem;
  }
  .back-menu-slider .back-menu-slider-content {
    background: #000000;
    background-color: rgba(0, 0, 0, 0.6);
    color: #fff;
    padding: 1rem 0;
  }
  .menu-slider-title {
    font-family: $headerFontStack;
    font-size: 4rem;
    color: white;
  }
  .categories-slider-item {
    display: inline-block;
    text-align: center;
    margin: 0 0.5rem;
    a {
      color: white;
      font-size: 100%;
      font-weight: 700;
      img {
        @include transition(opacity .25s);
        border-radius: 50%;
        opacity: .8;
      }
    }
    &:hover, &:focus {
      a img {
        opacity: 1;
      }
    }
  } 
  .page-header__description--menus {
    text-align:  center;
  }

.section_title--allMenus {
  text-align: center;
  font-family: $bodyFontStack;
  color: black;
}
.menus-thumbnail {
  position: relative;
  width: 175px;
  height: 175px;
  margin: 0 auto;
  background-color: #bbb;
  border-radius: 50%;
  &:hover, &:focus {
    .menus-thumbnail__legend {
      background-color: rgba(0,0,0, .5);
      span {
        opacity: 1;
      }
    }
  }
}
.menus-thumbnail--all img {
  border-radius: 50%;
}
.menus-thumbnail__legend {
  @include transition(background-color .25s);
  background-color: transparent;
  position: absolute;
  border-radius: 50%;
  width: 100%;
  height: 100%;
  top: 0;
  left: 0;

  span {
    opacity: 0;
    color: white;
    font-size: 1.2rem;
    font-weight: 700;
    line-height: 175px;
  }
}
// menu page
.section__title--menu {
  display: inline-block;
  margin-right: 2rem;
}
.anchor-items {
  display: inline-block;
}
.anchor-item {
  display: inline-block;
  vertical-align: middle;
  margin: 0.25em 0.5em;
}
.anchor-scroll {
  font-size: 0.7rem;
  text-decoration: none;
  color: $body-color;
  img {
    background-color: #bbb;
    border-radius: 50%;
    width: 100px;
    height: 100px;
  }
}
.anchor-image {
  border-radius: 50%;
  width: 100px;
}
.food {
  overflow: hidden;
  _overflow: visible;
  padding: 1rem 0;
  position: relative;
  zoom: 1;
  border-top: 1px solid #ddd;
}
  .food__image {
    margin: 0 auto;
    display: block;
    margin-bottom: $gutter / 2;
  }

  .food-body {
  }
    .food__title {
      font-family: $bodyFontStack;
      font-size: 1.2em;
      margin-bottom: .25em;
      font-weight: 700;
    }
    .food__summary {
      font-size: 1em;
      color: black;
      margin-bottom: .5em;
    }
    .foot-prices {

    }
.food--featured {
  .icon-star {
    color: $featured-star-color;
  }
  .food__title {
    font-weight: bold;
  }
}

.prices {
  margin-bottom: 1em;
  text-align: right;
}
  .price__label {
    color: $price-label-color;
  }
  .price__value {
    color: $price-value-color;
    font-weight: 700;
  }


.back-to-menu {
    text-align: center;
    &:before {
      color: $back-to-menu-link-color;
      content: "\2191";
      display: inline-block;
    }
  }
    .back-to-menu__link {
      color: $back-to-menu-link-color;
      text-decoration: none;
      &:hover, &:focus {
        color: $back-to-menu-link-hover-color;
      }
    }


// Tags
.food__tags {
}
  .tag {
    font-size: .95em;
    border-radius: $radius;
    padding: .2em .4em;
  }
    .tag--vegan, .tag--vegetarian {
      background-color: $label-vegetarian-background;
      color: $label-vegetarian-color;
    }
    .tag--new {
      background-color: $label-new-background;
      color: $label-new-color;
    }
    .tag--no-pork {
      background-color: $label-no-pork-background;
      color: $label-no-pork-color;
    }
    .tag--no-meat {
      background-color: $label-no-meat-background;
      color: $label-no-meat-color;
    }

// Gallery

.restaurant-image {
  position: relative;
  background-repeat: no-repeat;
  background-size: cover;
  text-align: center;
  color: white;
  background-color: black;
  margin-bottom: 2rem;
}
.restaurant-image-darken {
  background-color: rgba(0,0,0,0.5);
  padding: 8% 0 6.5% 0;
}
.restaurant-name {
  font-family: $bodyFontStack;
  font-size: 2rem;
  font-weight: 700;
}
.restaurant-slogan {
  font-family: $headerFontStack;
  font-size: 3rem;
}
.restaurant-city {
  font-family: $bodyFontStack;
  font-size: 100%;
}
.restaurant-gallery {
  margin-bottom: 2rem;
}
  .gallery__item {
    position: relative;
    margin-bottom: 1em;
    &:hover, &:focus {
      .photo__caption {
        color: $accent-color;
      }
    }
  }
    .gallery__img {
      img {
        @extend .auto;
      }
    }
    .photo__caption {
      @include transition(color .25s);
      color: white;
      position: absolute;
      bottom: 8px;
      left: 0;
      right: 0;
      padding: .5em;
      font-size: 1rem;
      font-weight: 700;
      background-color: rgba(0,0,0, .65);
    }
    

// newsletter page
.newsletter-wrapper {
  text-align: center;
}

.newsletter-title {
  font-family: $headerFontStack;
  font-size: 3rem;
}

.newsletter-description {
  font-family: $bodyFontStack;
  font-size: 1rem;
}
// contact page
.page-header--contact,
.page-header--newsletter,
.page-header--error,
.page-header--legal,
.page-header--page {
  text-align: center;
}
.adress-contact-wrapper,
.tel-contact-wrapper {
  color: black;
  font-size: 1rem;
  padding: 1rem 0;
  h3, p {
    font-family: $bodyFontStack;
  }
}

/*Ribbon CSS*/
.section-all-menus {
  text-align: center;
}
.ribbon {
  display: inline-block;
  margin: 20px 0;
  position: relative;
}
.ribbon path {
  fill: $accent-color;
}
.ribbon svg {
  width: 100%;
  height: 58px;
}
.ribbon span {
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  text-align: center;
  line-height: 68px;
  font-size: 20px;
  color: $ribbon-text-color;
  text-transform: uppercase;
  font-weight: 800;
}
.ribbon-small {
  height: 55px;
  line-height: 15px;
}
.ribbon-small span {
  height: 41px;
  line-height: 64px;
  font-size: 14px;
}
.wrap-reservation .ribbon {
  margin-top: 27px;
}
.ribbon a {
  text-decoration: none;
}

/**
 * Pure CSS Lightbox
 * @see http://tympanus.net/codrops/2011/12/26/css3-lightbox/
 */
.lb-overlay{
  width: 0px;
  height: 0px;
  position: fixed;
  overflow: hidden;
  left: 0px;
  top: 0px;
  padding: 0px;
  z-index: 99;
  text-align: center;
  background: rgba(255,255,255, .94);
}
.lb-overlay > div {
  position: relative;
  width: 100%;
  margin: 50px auto 0px auto;
}
.lb-overlay div h3 {
  text-align: center;
  font-size: 2.5rem;
  width: 100%;
}
.lb-overlay a.lb-close{
  z-index: 1001;
  position: absolute;
  top: 5px;
  text-align: center;
  height: 23px;
  overflow: hidden;
  text-decoration: none;
}
.lb-overlay img{
  /* height: 100%; For Opera max-height does not seem to work */
  max-height: 100%;
  position: relative;
  opacity: 0;
  top: 30px;
  filter: alpha(opacity=0); /* internet explorer */
    -ms-filter:"progid:DXImageTransform.Microsoft.Alpha(opacity=0)"; /*IE8*/
  -webkit-transition: opacity 0.5s linear;
  -moz-transition: opacity 0.5s linear;
  -o-transition: opacity 0.5s linear;
  -ms-transition: opacity 0.5s linear;
  transition: opacity 0.5s linear;
}
.lb-prev, .lb-next{
  position: absolute;
  width: 24px;
  height: 25px;
  left: 50%;
  opacity: 0.8;
  filter: alpha(opacity=80); /* internet explorer */
    -ms-filter:"progid:DXImageTransform.Microsoft.Alpha(opacity=80)"; /*IE8*/
}
.lb-prev:hover, .lb-next:hover{
  opacity: 1;
  filter: alpha(opacity=99); /* internet explorer */
    -ms-filter:"progid:DXImageTransform.Microsoft.Alpha(opacity=99)"; /*IE8*/
}
.lb-prev{
  color: $body-color;
  margin-left: -30px;
  text-decoration: none;
}
.lb-next{
  color: $body-color;
  margin-left: 6px;
  text-decoration: none;
}
.lb-overlay:target {
  width: 100%;
  height: auto;
  bottom: 0px;
  right: 0px;
}
.lb-overlay:target img,
.lb-overlay:target a.lb-close{
  margin: 0 auto;
  opacity: 1;
  filter: alpha(opacity=99); /* internet explorer */
    -ms-filter:"progid:DXImageTransform.Microsoft.Alpha(opacity=99)"; /*IE8*/
}
/* 
  100% Height for Opera as the max-height seems to be ignored, not optimal for large screens 
  http://bricss.net/post/11230266445/css-hack-to-target-opera 
*/
x:-o-prefocus, .lb-overlay img {
    height: 100%;
}

// ==============================================================================
// #Icomoon font
// https://icomoon.io/app/
// ==============================================================================

@font-face {
  font-family: 'icomoon';
  src:url("{{ 'restoready-flavour.eot' | asset_url }}");
  src:url("{{ 'restoready-flavour.eot' | asset_url }}") format('embedded-opentype'),
    url("{{ 'restoready-flavour.woff' | asset_url }}") format('woff'),
    url("{{ 'restoready-flavour.ttf' | asset_url }}") format('truetype'),
    url("{{ 'restoready-flavour.svg' | asset_url }}") format('svg');
  font-weight: normal;
  font-style: normal;
}

[class^="icon-"], [class*=" icon-"] {
  font-family: 'icomoon';
  speak: none;
  font-style: normal;
  font-weight: normal;
  font-variant: normal;
  text-transform: none;
  line-height: 1;

  /* Better Font Rendering =========== */
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

.icon-tripavisor:before {
  content: "\e600";
}
.icon-times:before {
  content: "\e601";
}
.icon-chevron-right:before {
  content: "\e602";
}
.icon-chevron-left:before {
  content: "\e603";
}
.icon-star:before {
  content: "\e604";
}
.icon-warning:before {
  content: "\e605";
}
.icon-input-checked:before {
  content: "\e606";
}
.icon-social-twitter:before {
  content: "\e607";
}
.icon-social-facebook:before {
  content: "\e608";
}
.icon-phone:before {
  content: "\e609";
}
.icon-envelope:before {
  content: "\f0e0";
}


/**
 * BxSlider v4.1.2 - Fully loaded, responsive content slider
 * http://bxslider.com
 *
 * Written by: Steven Wanderski, 2014
 * http://stevenwanderski.com
 * (while drinking Belgian ales and listening to jazz)
 *
 * CEO and founder of bxCreative, LTD
 * http://bxcreative.com
 */

/** RESET AND LAYOUT
===================================*/

.bx-wrapper {
  position: relative;
  margin: 0 auto 60px;
  padding: 0;
  *zoom: 1;
}

.bx-wrapper img {
  display: block;
}

/** THEME
===================================*/

.bx-wrapper .bx-viewport {
  -moz-box-shadow: 0 0 5px #ccc;
  -webkit-box-shadow: 0 0 5px #ccc;
  box-shadow: 0 0 5px #ccc;
  background: #fff;
  
  /*fix other elements on the page moving (on Chrome)*/
  -webkit-transform: translatez(0);
  -moz-transform: translatez(0);
      -ms-transform: translatez(0);
      -o-transform: translatez(0);
      transform: translatez(0);
}

.bx-wrapper .bx-pager,
.bx-wrapper .bx-controls-auto {
  position: absolute;
  bottom: -30px;
  width: 100%;
}

/* LOADER */

.bx-wrapper .bx-loading {
  min-height: 50px;
  background: url("{{ 'bx_loader.gif' | asset_url }}") center center no-repeat #fff;
  height: 100%;
  width: 100%;
  position: absolute;
  top: 0;
  left: 0;
  z-index: 2000;
}

/* PAGER */

.bx-wrapper .bx-pager {
  text-align: center;
  font-size: .85em;
  font-family: Arial;
  font-weight: bold;
  color: #666;
  padding-top: 20px;
}

.bx-wrapper .bx-pager .bx-pager-item,
.bx-wrapper .bx-controls-auto .bx-controls-auto-item {
  display: inline-block;
  *zoom: 1;
  *display: inline;
}

.bx-wrapper .bx-pager.bx-default-pager a {
  background: #666;
  text-indent: -9999px;
  display: block;
  width: 20px;
  height: 20px;
  margin: 0 5px;
  outline: 0;
  -moz-border-radius: 10px;
  -webkit-border-radius: 10px;
  border-radius: 10px;
}

.bx-wrapper .bx-pager.bx-default-pager a:hover,
.bx-wrapper .bx-pager.bx-default-pager a.active {
  background: #000;
}

/* DIRECTION CONTROLS (NEXT / PREV) */

.bx-wrapper .bx-prev {
  left: 10px;
  background: url("{{ 'controls-arrow.png' | asset_url }}") no-repeat -60px 0px;
}

.bx-wrapper .bx-next {
  right: 10px;
  background: url("{{ 'controls-arrow.png' | asset_url }}") no-repeat 0px 0px;
}

.bx-wrapper .bx-controls-direction a {
  position: absolute;
  top: 50%;
  margin-top: 115px;
  outline: 0;
  width: 60px;
  height: 60px;
  text-indent: -9999px;
  z-index: 9999;
}

.bx-wrapper .bx-controls-direction a.disabled {
  display: none;
}

/* PAGER WITH AUTO-CONTROLS HYBRID LAYOUT */

.bx-wrapper .bx-controls.bx-has-controls-auto.bx-has-pager .bx-pager {
  text-align: left;
  width: 80%;
}

.bx-wrapper .bx-controls.bx-has-controls-auto.bx-has-pager .bx-controls-auto {
  right: 0;
  width: 35px;
}

/* IMAGE CAPTIONS */

.bx-wrapper .bx-caption {
  position: absolute;
  bottom: 0;
  left: 0;
  background: #666\9;
  background: rgba(80, 80, 80, 0.75);
  width: 100%;
}

.bx-wrapper .bx-caption span {
  color: #fff;
  font-family: Arial;
  display: block;
  font-size: .85em;
  padding: 10px;
}

// terms
.wrapper-terms {
  padding: 0.5rem;
  h2, h3 {
    font-family: $bodyFontStack;
  }
}

// ==============================================================================
// #Breakpoints
// ==============================================================================

// Medium
@include at-query($min, $small) {
  body,
  input,
  textarea,
  select {
    font-size: 112.5%;
  }
  .button {
    width: 350px;
  }
  .food {
    padding: 1.5rem 0;
  }
    .food__image {
      margin: 0 auto;
      display: block;
      margin-bottom: 0;
      margin-right: $gutter;
      float: left;
    }
    .bx-wrapper .bx-controls-direction a {
      margin-top: 8px;
    }

  .directions, .wrapp-see-location {
    min-height: 310px;
  }
  .localisation-title {
    font-size: 4rem;
  }

  .gallery__item {
    display: inline-block;
    vertical-align: top;
    width: 45%;
    margin: 2%;
  }

}

// Large
@include at-query($min, $medium) {
  body,
  input,
  textarea,
  select {
    font-size: 125%;
  }

  .main-nav__item {
    position: relative;
  }
  .main-nav__item a {
    color: $nav-link-color;
  }
  .main-sub-nav__items {
    background-color: mix($accent-color, white, 25%);
    padding: 0;
    display: none;
    opacity: 0;
    visibility: hidden;
    margin: 0;
    position: absolute;
    left: 0;
    z-index: 2;
  }
  .main-sub-nav__item {
    display: block;
    text-align: left;
    margin-bottom: 0;
    border-left: 4px solid transparent;
    border-right: 4px solid transparent;
    &:hover, &:focus {
      background-color: $accent-color;
    }
  }
  .main-sub-nav__item--current {
    border-left-color: $accent-color;
  }
  .main-sub-nav__link{
    display: inline-block;
    font-size: 1em;
    text-decoration: none;
    padding: .5em;
    font-weight: 700;
    color: $nav-link-color;
  }
  .main-nav__item:hover {
    background-color: $accent-color;
    .main-sub-nav__items {
      display: block;
      opacity: 1;
      visibility: visible;
    }
  }

  .main-nav__items-left {
    margin: 0;
    display: inline-block;
    vertical-align: middle;
    max-width: 40%;
  }
  .main-nav__items-right {
    margin: 0;
    display: inline-block;
    vertical-align: middle;
    max-width: 40%;
  }
    .main-nav__item {
      @include transition(background-color .25s);
      display: inline-block;
      font-size: 1rem;
      &:hover {
          background-color: $accent-color;
      }
    }
    .main-nav__item--current {
        border-bottom: 4px solid $accent-color;
    }
      .main-nav__link {
        display: inline-block;
        font-size: 1em;
        text-decoration: none;
        padding: .5em;
        font-weight: 700;
        color: $nav-link-color;
      }
    .main-sub-nav__item {
      @include transition(background-color .25s);
    }
  .main-nav__item--language {
    &:hover, &:focus {
      background-color: mix($accent-color, white, 50%);
    }
  }
  .menus__item--home {
    float: left;
    display: block;
    width: 33.333333%;
    height: auto;
    padding-right: 25px;
  }

  .block-grid {
    &>li {
      &:nth-of-type(3n+1) {
        clear: left;
      }
    }
  }

  .menus__children {
    overflow: hidden;
    &>li {
      float: left;
      width: 33.3333333%;
      &:nth-of-type(3n+1) {
        clear: left;
      }
    }
    .submenu-full-width {
      width: 100% !important;
    }
    .submenu-half-width {
      width: 50% !important;
    }
  }

  .food {
    margin: 0 10rem;
  }

  .restaurant-slogan {
    font-size: 4rem;
  }
  .gallery__item {
    width: 30%;
    margin: 1%;
  }

    .input--name,
    .input--mail {
      width: 47%;
    }
    .input--name {
      float: left;
    }
    .input--mail {
      float: right;
    }

    // contact page 
    .form-contact {
      float: left;
      width: 50%;
    }
    .contact-informations {
      float: right;
      width: 50%;
      padding-left: 8rem;
    }
    // terms
    .wrapper-terms {
      width: 50%;
      margin: 0 auto;
    }
    .bx-wrapper .bx-controls-direction a {
      margin-top: -16px;
    }
    /**
   * Pure CSS Lightbox
   * @see http://tympanus.net/codrops/2011/12/26/css3-lightbox/
   */
  .lb-overlay a.lb-close{
    top: 45px;
  }
  .lb-overlay:target {
    width: auto;
    padding: 80px 100px 120px 100px;
  }

  .wrapp-see-location {
    padding: 45px 0;
  }
}
