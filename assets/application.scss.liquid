// ==============================================================================
// #Colors
// ==============================================================================
$body-background: {{ theme_settings.body-background }};
$body-color: {{ theme_settings.body-color }};

$restaurant-name-color: {{ theme_settings.restaurant-name-color }};
$restaurant-summary-color: {{ theme_settings.restaurant-summary-color }};
$slogan-color: {{ theme_settings.slogan-color }};

$phone-number-link-color: {{ theme_settings.phone-number-link-color }};
$phone-number-link-hover-color: {{ theme_settings.phone-number-link-hover-color }};

$main-nav-background: {{ theme_settings.main-nav-background }};
$main-nav-link-color: {{ theme_settings.main-nav-link-color }};
$main-nav-link-hover-color: {{ theme_settings.main-nav-link-hover-color }};
$main-nav-link-background: {{ theme_settings.main-nav-link-background }};
$main-nav-link-hover-background: {{ theme_settings.main-nav-link-hover-background }};

$footer-background: {{ theme_settings.footer-background }};
$back-to-top-link-color: {{ theme_settings.back-to-top-link-color }};
$back-to-top-link-hover-color: {{ theme_settings.back-to-top-link-hover-color }};
$footer-subscribe-label-color: {{ theme_settings.footer-subscribe-label-color }};
$social-link-color: {{ theme_settings.social-link-color }};
$social-link-hover-color: {{ theme_settings.social-link-hover-color }};
$legal-link-color: {{ theme_settings.legal-link-color }};
$legal-link-hover-color: {{ theme_settings.legal-link-hover-color }};

// Forms
$form-legend-color: {{ theme_settings.form-legend-color }};
$form-label-color: {{ theme_settings.form-label-color }};
$form-field-background: {{ theme_settings.form-field-border-color }};
$form-field-border-color: {{ theme_settings.form-field-border-color }};

$input-hint-info-background: {{ theme_settings.input-hint-info-background }};
$input-hint-info-color: {{ theme_settings.input-hint-info-color }};
$input-hint-error-background: {{ theme_settings.input-hint-error-background }};
$input-hint-error-color: {{ theme_settings.input-hint-error-color }};

// Buttons
$button-background: {{ theme_settings.button-background }};
$button-color: {{ theme_settings.button-color }};
$button-hover-background: {{ theme_settings.button-hover-background }};
$button-hover-color: {{ theme_settings.button-hover-color }};

// Messages
$message-success-background: {{ theme_settings.message-success-background }};
$message-success-color:      {{ theme_settings.message-success-color }};
$message-error-background:   {{ theme_settings.message-error-background }};
$message-error-color:        {{ theme_settings.message-error-color }};

// Menus
$menu-title-color: {{ theme_settings.menu-title-color }};
$menu-description-color: {{ theme_settings.menu-description-color }};
$menu-link-color: {{ theme_settings.menu-link-color }};
$menu-link-hover-color: {{ theme_settings.menu-link-hover-color }};

$back-to-menu-link-color: {{ theme_settings.back-to-menu-link-color }};
$back-to-menu-link-hover-color: {{ theme_settings.back-to-menu-link-hover-color }};

// Foods
$food-separator-color: rgba(black, .075);
$food-summary-description-color: {{ theme_settings.food-summary-description-color }};
$featured-star-color: {{ theme_settings.featured-star-color }};

// Prices
$price-label-background: transparent;
$price-label-color: {{ theme_settings.price-label-color }};
$price-value-background: {{ theme_settings.price-value-background }};
$price-value-color: {{ theme_settings.price-value-color }};
$price-border-color: {{ theme_settings.price-border-color }};
$price-label-border-color: $price-border-color;
$prices-top-border-color: $price-border-color;
$price-value-border-color: $price-border-color;

// Tags
$label-vegetarian-background: {{ theme_settings.label-vegetarian-background }};
$label-vegetarian-color: {{ theme_settings.label-vegetarian-color }};
$label-new-background: {{ theme_settings.label-new-background }};
$label-new-color: {{ theme_settings.label-new-color }};

// Page
$page-header-background: {{ theme_settings.page-header-background }};
$page-header-title-color: {{ theme_settings.page-header-title-color }};
$page-header-description-color: {{ theme_settings.page-header-description-color }};
$page-body-link-color: {{ theme_settings.page-body-link-color }};
$page-body-link-hover-color: {{ theme_settings.page-body-link-hover-color }};

// Featured menus
$featured-menus-background:   {{ theme_settings.featured-menus-background }};
$featured-menus-title-color:  {{ theme_settings.featured-menus-title-color }};
$featured-menus-border-color: {{ theme_settings.featured-menus-border-color }};

// Featured foods
$featured-foods-background:   {{ theme_settings.featured-foods-background }};
$featured-foods-title-color:  {{ theme_settings.featured-foods-title-color }};
$featured-foods-border-color: {{ theme_settings.featured-foods-border-color }};

// Opening hours
$opening-hours-background:   {{ theme_settings.opening-hours-background }};
$opening-hours-title-color:  {{ theme_settings.opening-hours-title-color }};
$opening-hours-border-color: {{ theme_settings.opening-hours-border-color }};
$opening-hours-day-color:    {{ theme_settings.opening-hours-day-color }};
$opening-hours-shifts-color: {{ theme_settings.opening-hours-shifts-color }};

// Directions
$directions-background:   {{ theme_settings.directions-background }};
$directions-title-color:  {{ theme_settings.directions-title-color }};
$directions-border-color: {{ theme_settings.directions-border-color }};
$address-color: {{ theme_settings.address-color }};

// Restaurant photo gallery
$restaurant-gallery-background: {{ theme_settings.restaurant-gallery-background }};
$gallery-caption-color: {{ theme_settings.gallery-caption-color }};


// ==============================================================================
// #Box model
// ==============================================================================
*, input, :before, :after {
  -webkit-box-sizing:border-box;
  -moz-box-sizing:border-box;
  box-sizing:border-box;
}

// ==============================================================================
// #Breakpoint and Grid Variables
// ==============================================================================
$viewportIncrement: 1px;

$small: 480px;
$medium: 768px;
$large: 769px;

$postSmall: $small + $viewportIncrement;
$preMedium: $medium - $viewportIncrement;
$preLarge: $large - $viewportIncrement;

/* The following are dependencies of csswizardry grid */
$breakpoints: (
  'small' '(max-width: #{$small})',
  'medium' '(min-width: #{$postSmall}) and (max-width: #{$medium})',
  'medium-down' '(max-width: #{$medium})',
  'large' '(min-width: #{$large})'
);
$breakpoint-has-widths: ('small', 'medium', 'medium-down', 'large');
$breakpoint-has-push:  ('medium', 'medium-down', 'large');
$breakpoint-has-pull:  ('medium', 'medium-down', 'large');

// ==============================================================================
// #General Variables
// ==============================================================================


/* Width variables */
$siteWidth: 960px;
$gutter: 24px;
$gridGutter: 24px; // can be a %, but nested grids will have smaller margins because of it

/* Radius */
$radius: .25em;

/* Typography */
$headerFontStack: {{ theme_settings.header-font }};
$bodyFontStack: {{ theme_settings.body-font }};



// ==============================================================================
// #Sass Mixins
// ==============================================================================
.clearfix {
  &:after {
    content: "";
    display: table;
    clear: both; }
  *zoom: 1;
}
@mixin clearfix() {
  @extend .clearfix;
}

@mixin box-shadow($shadow) {
  -webkit-box-shadow: #{$shadow};
  -moz-box-shadow:    #{$shadow};
  box-shadow:         #{$shadow};
}

@mixin transition($transition:0.1s all) {
  -webkit-transition: #{$transition};
  -moz-transition: #{$transition};
  -o-transition: #{$transition};
  transition: #{$transition};
}

@mixin gradient($from, $to, $fallback) {
  background: $fallback;
  background: -moz-linear-gradient(top, $from 0%, $to 100%);
  background: -webkit-gradient(linear, left top, left bottom, color-stop(0%,$from), color-stop(100%,$to));
  background: -webkit-linear-gradient(top, $from 0%,$to 100%);
  background: -o-linear-gradient(top, $from 0%,$to 100%);
  background: -ms-linear-gradient(top, $from 0%,$to 100%);
  background: linear-gradient(top bottom, $from 0%,$to 100%);
}

@mixin backface($visibility: hidden) {
  -moz-backface-visibility: $visibility;
  -webkit-backface-visibility: $visibility;
  -o-backface-visibility: $visibility;
  backface-visibility: $visibility;
}



// ==============================================================================
// Dependency-free breakpoint mixin
// http://blog.grayghostvisuals.com/sass/sass-media-query-mixin/
// ==============================================================================
$min: min-width;
$max: max-width;
@mixin at-query ($constraint, $viewport1, $viewport2:null) {
  @if $constraint == $min {
    @media screen and ($min: $viewport1) {
      @content;
    }
  } @else if $constraint == $max {
    @media screen and ($max: $viewport1) {
      @content;
    }
  } @else {
    @media screen and ($min: $viewport1) and ($max: $viewport2) {
      @content;
    }
  }
}

// ==============================================================================
// #Reset
// ==============================================================================
html,body,div,span,applet,object,iframe,h1,h2,h3,h4,h5,h6,p,blockquote,pre,a,abbr,acronym,address,big,cite,code,del,dfn,em,img,ins,kbd,q,s,samp,small,strike,strong,sub,sup,tt,var,b,u,i,center,dl,dt,dd,ol,ul,li,fieldset,form,label,legend,table,caption,tbody,tfoot,thead,tr,th,td,article,aside,canvas,details,embed,figure,figcaption,footer,header,hgroup,menu,nav,output,ruby,section,summary,time,mark,audio,video{border:0;font-size:100%;font:inherit;vertical-align:baseline;margin:0;padding:0;}article,aside,details,figcaption,figure,footer,header,hgroup,menu,nav,section{display:block;}ol,ul{list-style:none;}blockquote,q{quotes:none;}blockquote:before,blockquote:after,q:before,q:after{content:none;}table{border-collapse:collapse;border-spacing:0;}code,pre{background-color:#faf7f5;font-family:Consolas,monospace;border:0 none;padding:0 2px;color:#51ab62;}pre{overflow:auto;padding:0.5em;margin:0 0 1em;}


// ==============================================================================
// #Grid Setup
//
// - Based on csswizardry grid, but with floated columns and a fixed gutter size
// - Breakpoints defined above, under #Breakpoint and Grid Variables
// - Note the inclusion of .grid-uniform to take care of clearfixes on evenly sized grid-items
// ==============================================================================
$responsive:         true;
$mobile-first:       true;
$use-silent-classes: false;
$push:               true;
$pull:               false;

/* Force clearfix on grids */
.grid,
.grid-uniform {
  @include clearfix;
}

/* Manual grid-item clearfix */
.grid-item.clear {
  clear: both;
}

$class-type: unquote(".");

@if $use-silent-classes == true {
  $class-type: unquote("%");
}

@mixin grid-media-query($media-query) {
  $breakpoint-found: false;

  @each $breakpoint in $breakpoints {
    $name: nth($breakpoint, 1);
    $declaration: nth($breakpoint, 2);

    @if $media-query == $name and $declaration {
      $breakpoint-found: true;

      @media only screen and #{$declaration} {
        @content;
      }
    }
  }

  @if $breakpoint-found == false{
    @warn "Breakpoint '#{$media-query}' does not exist";
  }
}


/**
 * Drop relative positioning into silent classes which can't take advantage of
 * the `[class*="push--"]` and `[class*="pull--"]` selectors.
 */
@mixin silent-relative() {
  @if $use-silent-classes == true{
    position:relative;
  }
}


/*------------------------------------*\
  $GRID SETUP
\*------------------------------------*/
/**
 * 1. Allow the grid system to be used on lists.
 * 2. Remove any margins and paddings that might affect the grid system.
 * 3. Apply a negative `margin-left` to negate the columns' gutters.
 */
#{$class-type}grid,
#{$class-type}grid-uniform {
  list-style:none;
  margin:0;
  padding:0;
  margin-left:-$gridGutter;
}

#{$class-type}grid-item {
  float: left;
  padding-left:$gridGutter;
  vertical-align:top;
  @if $mobile-first == true {
    width:100%;
  }
  -webkit-box-sizing:border-box;
  -moz-box-sizing:border-box;
  box-sizing:border-box;
}

/**
 * Reversed grids allow you to structure your source in the opposite
 * order to how your rendered layout will appear.
 */
#{$class-type}grid--rev {
  @extend #{$class-type}grid;
  direction:rtl;
  text-align:left;

  > #{$class-type}grid-item {
    direction:ltr;
    text-align:left;
    float: right;
  }
}

/* Gutterless grids have all the properties of regular grids, minus any spacing. */
#{$class-type}grid--full {
  @extend #{$class-type}grid;
  margin-left:0;

  > #{$class-type}grid-item {
    padding-left:0;
  }
}

/* Align the entire grid to the right. */
#{$class-type}grid--right {
  @extend #{$class-type}grid;
  direction:rtl;
  text-align:right;

  > #{$class-type}grid-item {
    direction:ltr;
    float: right;
    text-align:left;
  }
}

/* Centered grids align grid items centrally without needing to use push or pull classes. */
#{$class-type}grid--center {
  @extend #{$class-type}grid;
  text-align:center;

  > #{$class-type}grid-item {
    text-align:left;
  }
}

/*------------------------------------*\
  $WIDTHS
\*------------------------------------*/
/**
 * Create our width classes, prefixed by the specified namespace.
 */
@mixin device-type($namespace:"") {

  /** Whole */
  #{$class-type}#{$namespace}one-whole       { width:100%; }

  /* Halves */
  #{$class-type}#{$namespace}one-half        { width:50%; }

  /* Thirds */
  #{$class-type}#{$namespace}one-third       { width:33.333%; }
  #{$class-type}#{$namespace}two-thirds      { width:66.666%; }

  /* Quarters */
  #{$class-type}#{$namespace}one-quarter     { width:25%; }
  #{$class-type}#{$namespace}two-quarters    { @extend #{$class-type}#{$namespace}one-half; }
  #{$class-type}#{$namespace}three-quarters  { width:75%; }

  /* Fifths */
  #{$class-type}#{$namespace}one-fifth       { width:20%; }
  #{$class-type}#{$namespace}two-fifths      { width:40%; }
  #{$class-type}#{$namespace}three-fifths    { width:60%; }
  #{$class-type}#{$namespace}four-fifths     { width:80%; }

  /* Sixths */
  #{$class-type}#{$namespace}one-sixth       { width:16.666%; }
  #{$class-type}#{$namespace}two-sixths      { @extend #{$class-type}#{$namespace}one-third; }
  #{$class-type}#{$namespace}three-sixths    { @extend #{$class-type}#{$namespace}one-half; }
  #{$class-type}#{$namespace}four-sixths     { @extend #{$class-type}#{$namespace}two-thirds; }
  #{$class-type}#{$namespace}five-sixths     { width:83.333%; }

  /* Eighths */
  #{$class-type}#{$namespace}one-eighth      { width:12.5%; }
  #{$class-type}#{$namespace}two-eighths     { @extend #{$class-type}#{$namespace}one-quarter; }
  #{$class-type}#{$namespace}three-eighths   { width:37.5%; }
  #{$class-type}#{$namespace}four-eighths    { @extend #{$class-type}#{$namespace}one-half; }
  #{$class-type}#{$namespace}five-eighths    { width:62.5%; }
  #{$class-type}#{$namespace}six-eighths     { @extend #{$class-type}#{$namespace}three-quarters; }
  #{$class-type}#{$namespace}seven-eighths   { width:87.5%; }

  /* Tenths */
  #{$class-type}#{$namespace}one-tenth       { width:10%; }
  #{$class-type}#{$namespace}two-tenths      { @extend #{$class-type}#{$namespace}one-fifth; }
  #{$class-type}#{$namespace}three-tenths    { width:30%; }
  #{$class-type}#{$namespace}four-tenths     { @extend #{$class-type}#{$namespace}two-fifths; }
  #{$class-type}#{$namespace}five-tenths     { @extend #{$class-type}#{$namespace}one-half; }
  #{$class-type}#{$namespace}six-tenths      { @extend #{$class-type}#{$namespace}three-fifths; }
  #{$class-type}#{$namespace}seven-tenths    { width:70%; }
  #{$class-type}#{$namespace}eight-tenths    { @extend #{$class-type}#{$namespace}four-fifths; }
  #{$class-type}#{$namespace}nine-tenths     { width:90%; }

  /* Twelfths */
  #{$class-type}#{$namespace}one-twelfth     { width:8.333%; }
  #{$class-type}#{$namespace}two-twelfths    { @extend #{$class-type}#{$namespace}one-sixth; }
  #{$class-type}#{$namespace}three-twelfths  { @extend #{$class-type}#{$namespace}one-quarter; }
  #{$class-type}#{$namespace}four-twelfths   { @extend #{$class-type}#{$namespace}one-third; }
  #{$class-type}#{$namespace}five-twelfths   { width:41.666% }
  #{$class-type}#{$namespace}six-twelfths    { @extend #{$class-type}#{$namespace}one-half; }
  #{$class-type}#{$namespace}seven-twelfths  { width:58.333%; }
  #{$class-type}#{$namespace}eight-twelfths  { @extend #{$class-type}#{$namespace}two-thirds; }
  #{$class-type}#{$namespace}nine-twelfths   { @extend #{$class-type}#{$namespace}three-quarters; }
  #{$class-type}#{$namespace}ten-twelfths    { @extend #{$class-type}#{$namespace}five-sixths; }
  #{$class-type}#{$namespace}eleven-twelfths { width:91.666%; }

}

/**
  * Create a clearfix helper on uniform grids
  */
@mixin clearfix-helper($namespace:"") {

  .grid-uniform {
    #{$class-type}#{$namespace}one-half:nth-child(2n+1),
    #{$class-type}#{$namespace}one-third:nth-child(3n+1),
    #{$class-type}#{$namespace}one-quarter:nth-child(4n+1),
    #{$class-type}#{$namespace}one-fifth:nth-child(5n+1),
    #{$class-type}#{$namespace}one-sixth:nth-child(6n+1),
    #{$class-type}#{$namespace}two-sixths:nth-child(3n+1),
    #{$class-type}#{$namespace}three-sixths:nth-child(2n+1),
    #{$class-type}#{$namespace}two-eighths:nth-child(4n+1),
    #{$class-type}#{$namespace}four-eighths:nth-child(2n+1),
    #{$class-type}#{$namespace}five-tenths:nth-child(2n+1),
    #{$class-type}#{$namespace}one-twelfth:nth-child(12n+1),
    #{$class-type}#{$namespace}two-twelfths:nth-child(6n+1),
    #{$class-type}#{$namespace}three-twelfths:nth-child(4n+1),
    #{$class-type}#{$namespace}four-twelfths:nth-child(3n+1),
    #{$class-type}#{$namespace}six-twelfths:nth-child(2n+1)     { clear:both; }
  }

}


/*------------------------------------*\
  $HELPERS
\*------------------------------------*/
/**
 * Create helper show/hide classes around our breakpoints.
 */
@mixin device-helper($namespace:"") {

  #{$class-type}#{$namespace}show        { display:block!important; }
  #{$class-type}#{$namespace}hide        { display:none!important; }

  #{$class-type}#{$namespace}text-left   { text-align:left!important; }
  #{$class-type}#{$namespace}text-right  { text-align:right!important; }
  #{$class-type}#{$namespace}text-center { text-align:center!important; }

  #{$class-type}#{$namespace}left        { float:left!important; }
  #{$class-type}#{$namespace}right       { float:right!important; }

}


/* Our regular, non-responsive width and helper classes. */
@include device-type();
@include device-helper();

/* Our responsive classes, if we have enabled them. */
@if $responsive == true {

  @each $name in $breakpoint-has-widths {
    @include grid-media-query($name) {
      @include device-type('#{$name}--');
      @include device-helper('#{$name}--');
      @include clearfix-helper('#{$name}--');
    }
  }

}


/*------------------------------------*\
  $PUSH
\*------------------------------------*/
/**
 * Push classes, to move grid items over to the right by certain amounts.
 */
@mixin push-setup($namespace: "") {

  /* Whole */
  #{$class-type}push--#{$namespace}one-whole       { left:100%; @include silent-relative(); }

  /* Halves */
  #{$class-type}push--#{$namespace}one-half        { left:50%; @include silent-relative(); }

  /* Thirds */
  #{$class-type}push--#{$namespace}one-third       { left:33.333%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-thirds      { left:66.666%; @include silent-relative(); }

  /* Quarters */
  #{$class-type}push--#{$namespace}one-quarter     { left:25%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-quarters    { @extend #{$class-type}push--#{$namespace}one-half; }
  #{$class-type}push--#{$namespace}three-quarters  { left:75%; @include silent-relative(); }

  /* Fifths */
  #{$class-type}push--#{$namespace}one-fifth       { left:20%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-fifths      { left:40%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}three-fifths    { left:60%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}four-fifths     { left:80%; @include silent-relative(); }

  /* Sixths */
  #{$class-type}push--#{$namespace}one-sixth       { left:16.666%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-sixths      { @extend #{$class-type}push--#{$namespace}one-third; }
  #{$class-type}push--#{$namespace}three-sixths    { @extend #{$class-type}push--#{$namespace}one-half; }
  #{$class-type}push--#{$namespace}four-sixths     { @extend #{$class-type}push--#{$namespace}two-thirds; }
  #{$class-type}push--#{$namespace}five-sixths     { left:83.333%; @include silent-relative(); }

  /* Eighths */
  #{$class-type}push--#{$namespace}one-eighth      { left:12.5%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-eighths     { @extend #{$class-type}push--#{$namespace}one-quarter; }
  #{$class-type}push--#{$namespace}three-eighths   { left:37.5%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}four-eighths    { @extend #{$class-type}push--#{$namespace}one-half; }
  #{$class-type}push--#{$namespace}five-eighths    { left:62.5%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}six-eighths     { @extend #{$class-type}push--#{$namespace}three-quarters; }
  #{$class-type}push--#{$namespace}seven-eighths   { left:87.5%; @include silent-relative(); }

  /* Tenths */
  #{$class-type}push--#{$namespace}one-tenth       { left:10%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-tenths      { @extend #{$class-type}push--#{$namespace}one-fifth; }
  #{$class-type}push--#{$namespace}three-tenths    { left:30%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}four-tenths     { @extend #{$class-type}push--#{$namespace}two-fifths; }
  #{$class-type}push--#{$namespace}five-tenths     { @extend #{$class-type}push--#{$namespace}one-half; }
  #{$class-type}push--#{$namespace}six-tenths      { @extend #{$class-type}push--#{$namespace}three-fifths; }
  #{$class-type}push--#{$namespace}seven-tenths    { left:70%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}eight-tenths    { @extend #{$class-type}push--#{$namespace}four-fifths; }
  #{$class-type}push--#{$namespace}nine-tenths     { left:90%; @include silent-relative(); }

  /* Twelfths */
  #{$class-type}push--#{$namespace}one-twelfth     { left:8.333%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}two-twelfths    { @extend #{$class-type}push--#{$namespace}one-sixth; }
  #{$class-type}push--#{$namespace}three-twelfths  { @extend #{$class-type}push--#{$namespace}one-quarter; }
  #{$class-type}push--#{$namespace}four-twelfths   { @extend #{$class-type}push--#{$namespace}one-third; }
  #{$class-type}push--#{$namespace}five-twelfths   { left:41.666%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}six-twelfths    { @extend #{$class-type}push--#{$namespace}one-half; }
  #{$class-type}push--#{$namespace}seven-twelfths  { left:58.333%; @include silent-relative(); }
  #{$class-type}push--#{$namespace}eight-twelfths  { @extend #{$class-type}push--#{$namespace}two-thirds; }
  #{$class-type}push--#{$namespace}nine-twelfths   { @extend #{$class-type}push--#{$namespace}three-quarters; }
  #{$class-type}push--#{$namespace}ten-twelfths    { @extend #{$class-type}push--#{$namespace}five-sixths; }
  #{$class-type}push--#{$namespace}eleven-twelfths { left:91.666%; @include silent-relative(); }
}

@if $push == true {

  /* Not a particularly great selector, but the DRYest way to do things. */
  [class*="push--"]{ position:relative; }

  @include push-setup();

  @if $responsive == true {
    @each $name in $breakpoint-has-push {
      @include grid-media-query($name) {
        @include push-setup('#{$name}--');
      }
    }
  }

}


/*------------------------------------*\
  $PULL
\*------------------------------------*/
/* Pull classes, to move grid items back to the left by certain amounts. */
@mixin pull-setup($namespace: "") {
  /* Whole */
  #{$class-type}pull--#{$namespace}one-whole       { right:100%; @include silent-relative(); }

  /* Halves */
  #{$class-type}pull--#{$namespace}one-half        { right:50%; @include silent-relative(); }

  /* Thirds */
  #{$class-type}pull--#{$namespace}one-third       { right:33.333%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-thirds      { right:66.666%; @include silent-relative(); }

  /* Quarters */
  #{$class-type}pull--#{$namespace}one-quarter     { right:25%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-quarters    { @extend #{$class-type}pull--#{$namespace}one-half; }
  #{$class-type}pull--#{$namespace}three-quarters  { right:75%; @include silent-relative(); }

  /* Fifths */
  #{$class-type}pull--#{$namespace}one-fifth       { right:20%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-fifths      { right:40%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}three-fifths    { right:60%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}four-fifths     { right:80%; @include silent-relative(); }

  /* Sixths */
  #{$class-type}pull--#{$namespace}one-sixth       { right:16.666%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-sixths      { @extend #{$class-type}pull--#{$namespace}one-third; }
  #{$class-type}pull--#{$namespace}three-sixths    { @extend #{$class-type}pull--#{$namespace}one-half; }
  #{$class-type}pull--#{$namespace}four-sixths     { @extend #{$class-type}pull--#{$namespace}two-thirds; }
  #{$class-type}pull--#{$namespace}five-sixths     { right:83.333%; @include silent-relative(); }

  /* Eighths */
  #{$class-type}pull--#{$namespace}one-eighth      { right:12.5%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-eighths     { @extend #{$class-type}pull--#{$namespace}one-quarter; }
  #{$class-type}pull--#{$namespace}three-eighths   { right:37.5%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}four-eighths    { @extend #{$class-type}pull--#{$namespace}one-half; }
  #{$class-type}pull--#{$namespace}five-eighths    { right:62.5%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}six-eighths     { @extend #{$class-type}pull--#{$namespace}three-quarters; }
  #{$class-type}pull--#{$namespace}seven-eighths   { right:87.5%; @include silent-relative(); }

  /* Tenths */
  #{$class-type}pull--#{$namespace}one-tenth       { right:10%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-tenths      { @extend #{$class-type}pull--#{$namespace}one-fifth; }
  #{$class-type}pull--#{$namespace}three-tenths    { right:30%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}four-tenths     { @extend #{$class-type}pull--#{$namespace}two-fifths; }
  #{$class-type}pull--#{$namespace}five-tenths     { @extend #{$class-type}pull--#{$namespace}one-half; }
  #{$class-type}pull--#{$namespace}six-tenths      { @extend #{$class-type}pull--#{$namespace}three-fifths; }
  #{$class-type}pull--#{$namespace}seven-tenths    { right:70%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}eight-tenths    { @extend #{$class-type}pull--#{$namespace}four-fifths; }
  #{$class-type}pull--#{$namespace}nine-tenths     { right:90%; @include silent-relative(); }

  /* Twelfths */
  #{$class-type}pull--#{$namespace}one-twelfth     { right:8.333%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}two-twelfths    { @extend #{$class-type}pull--#{$namespace}one-sixth; }
  #{$class-type}pull--#{$namespace}three-twelfths  { @extend #{$class-type}pull--#{$namespace}one-quarter; }
  #{$class-type}pull--#{$namespace}four-twelfths   { @extend #{$class-type}pull--#{$namespace}one-third; }
  #{$class-type}pull--#{$namespace}five-twelfths   { right:41.666%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}six-twelfths    { @extend #{$class-type}pull--#{$namespace}one-half; }
  #{$class-type}pull--#{$namespace}seven-twelfths  { right:58.333%; @include silent-relative(); }
  #{$class-type}pull--#{$namespace}eight-twelfths  { @extend #{$class-type}pull--#{$namespace}two-thirds; }
  #{$class-type}pull--#{$namespace}nine-twelfths   { @extend #{$class-type}pull--#{$namespace}three-quarters; }
  #{$class-type}pull--#{$namespace}ten-twelfths    { @extend #{$class-type}pull--#{$namespace}five-sixths; }
  #{$class-type}pull--#{$namespace}eleven-twelfths { right:91.666%; @include silent-relative(); }
}

@if $pull == true {

  /* Not a particularly great selector, but the DRYest way to do things. */
  [class*="pull--"]{ position:relative; }

  @include pull-setup();

  @if $responsive == true{
    @each $name in $breakpoint-has-pull {
      @include grid-media-query($name) {
        @include pull-setup('#{$name}--');
      }
    }
  }

}


// ==============================================================================
// #Basic Styles
// ==============================================================================
.wrapper {
  @include clearfix();
  max-width: $siteWidth;
  margin: 0 auto;
  padding: 0 $gutter;
}


// ==============================================================================
// #Helper Classes
// ==============================================================================
.left { float: left; }
.right { float: right; }
.text-center { text-align: center; }
.text-right { text-align: right; }
.hidden { display: none; }
.hide-text {
  height: 1px;
  width: 1px;
  position: absolute;
  overflow: hidden;
  top: -10px;
}
.display-table {
  display: table;
}

.display-table-cell {
  display: table-cell;
  vertical-align: middle;
}


// ==============================================================================
// #Typography
// ==============================================================================
body,
input,
textarea,
button,
select {
  font-size: 87.5%; // Henseforth known as 1em (assumed at 14px)
  line-height: 1.6;
  font-family: $bodyFontStack;
  font-weight: 300;
  -webkit-font-smoothing: antialiased;
  -webkit-text-size-adjust: 100%;
}

h1, h2, h3, h4, h5, h6 {
  display: block;
  font-family: $headerFontStack;
  font-weight: normal;
  line-height: 1.4;
  a {
    font-weight: inherit;
    text-decoration: none;
  }
}

// Pixel to EM conversion tool http://pxtoem.com/
h1 { font-size: 3em; }       // 42px
h2 { font-size: 2.429em; }   // 34px
h3 { font-size: 2em; }       // 28px
h4 { font-size: 1.429em; }   // 20px
h5 { font-size: 1.143em; }   // 16px
h6 { font-size: 1em; }       // 14px

p { margin: 0 0 1em 0; }
p img { margin: 0; }

em { font-style: italic; }
strong { font-weight: bold; }
small { font-size: 80%; }
sup {
  position: relative;
  top: -0.5em;
  font-size: 60%;
}


// ==============================================================================
// #Lists
// ==============================================================================
ul, ol { margin-bottom: 1em; }
ul { list-style: none outside; }
ol { list-style: decimal; }
ol, ul.square, ul.disc { margin-left: 1em; }
ul.square { list-style: square outside; }
ul.disc { list-style: disc outside; }
ol.alpha { list-style: lower-alpha outside; }
li { margin-bottom: 0.25em; }
.inline-list li {
  display: inline-block;
  margin-bottom: 0;
}


// ==============================================================================
// #Images and Iframes
// ==============================================================================
img.auto,
.grid-item img,
.grid-item iframe {
  max-width: 100%;
  height: auto;
}

img.circle {
  border-radius: 50%;
}


// ==============================================================================
// #Forms
// ==============================================================================
.button,
button,
input[type="text"],
input[type="search"],
input[type="password"],
input[type="email"],
input[type="file"],
input[type="number"],
input[type="tel"],
input[type="submit"],
textarea {
  -webkit-appearance: none;
  -moz-appearance: none;
}

input,
textarea,
select,
fieldset {
  max-width: 100%;
}

input,
select,
textarea {
  font-size: 1em;
  line-height: 1.42;
  margin: 0;
  padding: .2rem;
}

input {
  display: inline-block;
  width: auto;
}

button,
input[type="submit"] {
  cursor: pointer;
}

/* Input width and border */
input[type="text"],
input[type="search"],
input[type="password"],
input[type="email"],
input[type="file"],
input[type="number"],
input[type="tel"],
textarea,
select {
  display: block;
  max-width: 100%;
  width: 100%;
}

textarea {
  min-height: 100px;
}

input[type="checkbox"],
input[type="radio"] {
  display: inline;
  margin: 0;
}

/* Form labels */
label,
legend {
  display: block;
  margin-bottom: 2px;
}

label[for] {
  cursor: pointer;
}

/* Horizontal Form */
form.form-horizontal,
.form-horizontal {
  margin-bottom: 0;

  input[type="text"],
  input[type="search"],
  input[type="password"],
  input[type="email"],
  input[type="file"],
  input[type="number"],
  input[type="tel"],
  textarea,
  select,
  label {
    display: inline-block;
    margin-bottom: 0;
    width: auto;
  }
}


// ==============================================================================
// #Links and Buttons
// ==============================================================================
.button {
  @include transition(background-color .25s);
  background-color: $button-background;
  border: 0;
  color: $button-color;
  cursor: pointer;
  display: inline-block;
  margin: 0;
  padding: .5em 2em;
  text-align: center;
  text-decoration: none;
  vertical-align: middle;
  white-space: nowrap;
  -webkit-appearance: none;

  &.button--small {
    font-size: 0.75em;
    padding: .25em .5em;
  }

  &.button--full {
    width: 100%;
  }

  &:hover,
  &:active,
  &:focus {
    background-color: $button-hover-background;
    color: $button-hover-color;
  }

  &[disabled],
  &.disabled {
    cursor: default;
  }
}


// ==============================================================================
// #Layout
// ==============================================================================
body {
  color: $body-color;
  background-color: $body-background;
}
a {
  text-decoration: none;
  color: $body-color;
}
.page {
  @extend .clearfix;
}

.main-header {
}
  .logo {
    width: 50%;
    margin: 1em auto;
  }
    .logo__link {
      display: block;
      text-decoration: none;
    }
      .logo__image {
        @extend .auto;
      }
  .restaurant-name {
    text-align: center;
  }
    .slogan {
      color: $slogan-color;
      font-size: .95em;
      font-style: italic;
      margin-bottom: .5em;
    }
    .restaurant-name__link {
      color: $restaurant-name-color;
      font-family: {{ theme_settings.header-font }};
      font-size: 2em;
      text-decoration: none;
    }
  .phone-number {
    margin-bottom: 1em;
    text-align: center;
  }
    .phone-number__link {
      color: {{ theme_settings.phone-number-link-color }};
      font-size: 1.25em;
      text-decoration: none;
      &:hover, &:focus {
        color: {{ theme_settings.phone-number-link-hover-color }};
      }
    }
      .phone-number__label {
      }

.main-nav {
  @include clearfix();
  background-color: $main-nav-background;
}
  .main-nav__items {
    margin: 0;
    text-align: center;
  }
    .main-nav__item {
      //display: inline-block;
      margin: 0;
      font-size: 1.25rem;
    }
      .main-nav__link {
        @include transition(background-color .25s);
        background-color: $main-nav-link-background;
        color: $main-nav-link-color;
        display: inline-block;
        font-size: 1em;
        text-decoration: none;
        padding: .5em;
        &:hover,
        &:focus {
          background-color: $main-nav-link-hover-background;
          color: $main-nav-link-hover-color;
        }
      }
  .main-nav__languages {
    margin: 0;
    text-align: center;
  }
    .main-nav__language {
      //display: inline-block;
      margin: 0;
      font-size: 1.25rem;
    }

.main-content {
  margin-bottom: 1em;
}
  .section__header {
  }
  .section__footer {
  }

.main-footer {
  background-color: $footer-background;
  border-top: 1px solid scale-color($footer-background, $lightness: -5%);
}
  .back-to-top {
    padding: $gutter;
    text-align: center;
  }
    .back-to-top__link {
      color: $back-to-top-link-color;
      text-decoration: none;
      &:hover, &:focus {
        color: $back-to-top-link-hover-color;
      }
    }
      .back-to-top__label {
      }
  .footer__subscribe {
    margin-bottom: 1em;
    .subscribe__label {
      color: $footer-subscribe-label-color;
    }
    .subscribe__submit {
    }
  }
  .footer__social {
  }
    .social__items {
      margin: .75em 0;
      text-align: center;
    }
      .social__item {
        display: inline-block;
      }
        .social__link {
          @include transition(color .25s);
          color: $social-link-color;
          display: inline-block;
          font-size: 2em;
          padding: .5em;
          text-decoration: none;
          &:hover, &:focus {
            color: $social-link-hover-color;
          }
        }
          .social__label {
            @extend .hide-text;
          }
  .footer__legal {
    margin-bottom: 1em;
    text-align: center;
  }
    .legal__link {
      color: $legal-link-color;
      text-decoration: none;
      &:hover, &:focus {
        color: $legal-link-hover-color;
      }
    }


// Forms
input[type="text"],
input[type="search"],
input[type="password"],
input[type="email"],
input[type="file"],
input[type="number"],
input[type="tel"],
textarea,
select {
  background-color: $form-field-background;
  border: 1px solid $form-field-border-color;
}

legend {
  color: $form-legend-color;
}

label {
  color: $form-label-color;
}

.field-group {
  margin-bottom: 1em;
}
  .field-group__legend {
    font-size: 1.5em;
    font-weight: 700;
    margin-bottom: 1em;
  }

.input,
.select,
.textarea {
  margin: 0 0 1em;
}
  .input-hint {
    padding: .15em .4em;
  }
  .input-hint--info {
    background: $input-hint-info-background;
    color: $input-hint-info-color;
  }
  .input-hint--error {
    background: $input-hint-error-background;
    color: $input-hint-error-color;
  }

.submit {
  text-align: center;
}

// Messages
.message {
  padding: .5em 1em;
}
.message--success {
  background-color: $message-success-background;
  color: $message-success-color;
}
.message--error {
  background-color: $message-error-background;
  color: $message-error-color;
}


// ==============================================================================
// #Modules
// ==============================================================================
.page-header {
  background-color: $page-header-background;
  padding: 1em 0;
}
  .page-header__header {
  }
    .page-header__title {
      color: $page-header-title-color;
    }
    .page-header__description {
      color: $page-header-description-color;
      p {
        font-size: 1.2em;
        margin-bottom: 0;
      }
    }
  .page-body {
    a {
      color: $page-body-link-color;
      border-bottom: 1px solid scale-color($page-body-link-color, $lightness: +75%);
      text-decoration: none;
      &:hover, &:focus {
        color: $page-body-link-hover-color;
        border-bottom: 1px solid scale-color($page-body-link-hover-color, $lightness: +75%);
      }
    }
  }

// Home
.restaurant-main-image {
  {% if restaurant.main_image?%}
    background-image: url({{ restaurant.main_image | custom_image_url: '1920x480#' }});
  {% endif %}
  background-color: rgba(0,0,0, .5);
  background-size: cover;
  background-position: center center;
  background-repeat: no-repeat;
}
.restaurant-summary {
  color: $restaurant-summary-color;
  padding-top: 4em;
  padding-bottom: 3em;
  text-align: center;
  p {
    font-size: 1.2em;
  }
}
.menus--featured {
  background-color: $featured-menus-background;
  margin-bottom: 1em;
  padding: 1em 0;
  .section__title {
    color: $featured-menus-title-color;
    border-bottom: 1px solid $featured-menus-border-color;
    margin-bottom: .75em;
  }
}
  .menus__menu-link {
    color: $menu-link-color;
    &:hover, &:focus {
      color: $menu-link-hover-color;
    }
  }
  .menus__children {
    list-style: none;
  }
    .menus__child {
      font-size: 1.2em;
      text-align: center;
      margin-bottom: 2rem;
    }
      .menus__child-link {
        color: $menu-link-color;
        text-decoration: none;
        &:hover, &:focus {
          color: $menu-link-hover-color;
        }
      }
  .menus__separator {
    border: none;
    border-top: medium solid $food-separator-color;
    color: $food-separator-color;
    margin: 1em 0;
    padding: 0;
  }

.foods--featured {
  background-color: $featured-foods-background;
  margin-bottom: 1em;
  padding: 1em 0;
  .section__title {
    color: $featured-foods-title-color;
    border-bottom: 1px solid $featured-foods-border-color;
    margin-bottom: .75em;
  }
}

.opening-hours {
  background-color: $opening-hours-background;
  margin-bottom: 1em;
  .section__title {
    color: $opening-hours-title-color;
    border-bottom: 1px solid $opening-hours-border-color;
    margin-bottom: 1em;
  }
}
  .opening-days {
    @extend .clearfix;
    margin-bottom: 1em;
  }
    .opening-day {
      color: $opening-hours-day-color;
      clear: left;
      float: left;
      font-size: 1.2em;
      width: 40%;
    }
    .opening-shifts {
      color: $opening-hours-shifts-color;
      font-size: 1.2em;
      margin-left: 50%;
    }
  .opening-hours__information {
    font-size: 1.1em;
    margin-bottom: 1em;
  }

.directions, .business-view {
  background-color: $directions-background;
  margin-bottom: 1em;
  .section__title {
    color: $directions-title-color;
    border-bottom: 1px solid $directions-border-color;
    margin-bottom: 1em;
  }
}
  .map {
    margin-bottom: 1em;
  }
    .map__image {
      @extend .auto;
    }
  .address {
    color: $address-color;
    font-size: 1.2em;
  }
  .directions__footer {
  }

.menu {
  padding-bottom: 1.5em;
}
  .menu--featured {

  }
  .menu__image-container {
    margin-bottom: 1em;
  }
    .menu__image {
      @extend .auto;
    }
  .menu__header {

  }
    .menu__title {
      color: $menu-title-color;
      margin-bottom: .5em;
      a {
        color: $menu-link-color;
        &:hover {
          color: $menu-link-hover-color;
        }
      }
    }
  .menu__description {
    color: $menu-description-color;
    font-size: 1.2em;
    margin-bottom: 1em;
  }


// http://www.stubbornella.org/content/2010/06/25/the-media-object-saves-hundreds-of-lines-of-code/
// all menus
.section_title--allMenus {
  text-align: center;
}
.menus-thumbnail--all img {
  border-radius: 50%;
}
// menu page
.section__title--menu {
  display: inline-block;
  margin-right: 2rem;
}
.anchor-items {
  display: inline-block;
}
.anchor-item {
  display: inline-block;
  vertical-align: middle;
  margin: 0.25em 0.5em;
}
.anchor-scroll {
  font-size: 0.7rem;
  text-decoration: none;
  color: $body-color;
}
.anchor-image {
  border-radius: 50%;
}
.food {
  overflow: hidden;
  _overflow: visible;
  padding: 0;
  position: relative;
  zoom: 1;
}
  .food__image {
    float: left;
    margin-bottom: $gutter / 2;
    margin-right: $gutter / 2;
  }

  .food__image--right {
    float: right;
    margin-left: $gutter / 2;
  }

  .food__image img,
  .food__image--right img {
    display: block;
  }

  .food-body {
  }
    .food__title {
      font-family: $bodyFontStack;
      font-size: 1.2em;
      margin-bottom: .25em;
    }
    .food__summary {
      font-size: 1em;
      color: $food-summary-description-color;
    }
    .foot-prices {

    }
  .food__separator {
    border: none;
    border-top: 1px solid $food-separator-color;
    color: $food-separator-color;
    margin: .675em 0;
    padding: 0;
  }
.food--featured {
  .icon-star {
    color: $featured-star-color;
  }
  .food__title {
    font-weight: bold;
  }
}

.prices {
  @extend .clearfix;
  border-top: 1px dotted $prices-top-border-color;
  margin-bottom: 1em;
}
  .price__label {
    clear: left;
    color: $price-label-color;
    background-color: $price-label-background;
    border-bottom: 1px dotted $price-label-border-color;
    float: left;
    width: 66%;
  }
  .price__value {
    color: $price-value-color;
    background-color: $price-value-background;
    border-bottom: 1px dotted $price-value-border-color;
    float: right;
    font-weight: 700;
    text-align: right;
    width: 33%;
    padding-right: .5em;
  }


.back-to-menu {
    text-align: center;
    &:before {
      color: $back-to-menu-link-color;
      content: "\2191";
      display: inline-block;
    }
  }
    .back-to-menu__link {
      color: $back-to-menu-link-color;
      text-decoration: none;
      &:hover, &:focus {
        color: $back-to-menu-link-hover-color;
      }
    }


// Tags
.food__tags {
}
  .tag {
    font-size: .95em;
    border-radius: $radius;
    padding: .2em .4em;
  }
    .tag--vegan, .tag--vegetarian {
      background-color: $label-vegetarian-background;
      color: $label-vegetarian-color;
    }
    .tag--new {
      background-color: $label-new-background;
      color: $label-new-color;
    }

// Gallery
.restaurant-gallery {
  background-color: $restaurant-gallery-background;
}
  .gallery__item {
    position: relative;
    margin-bottom: 1em;
  }
    .gallery__img {
      position: relative;
      img {
        display: block;
        max-width: 100%;
      }
    }
    .photo__caption {
      display: inline-block;
      vertical-align: middle;
      line-height: normal;
      word-break: break-word;
    }
    .photo__link {
      @include transition(background-color .5s);
      background-color: rgba(0, 0, 0, 0.5);
      color: $gallery-caption-color;
      font-weight: 700;
      line-height: 200px;
      position: absolute;
      width: 100%;
      height: 100%;
      display: block;
      overflow: hidden;
      text-align: center;
      text-decoration: none;
    }
  .gallery__item:hover .photo__link {
      background-color: rgba(0, 0, 0, 0.25);
  }

/**
 * Pure CSS Lightbox
 * @see http://tympanus.net/codrops/2011/12/26/css3-lightbox/
 */
.lb-overlay{
  width: 0px;
  height: 0px;
  position: fixed;
  overflow: hidden;
  left: 0px;
  top: 0px;
  padding: 0px;
  z-index: 99;
  text-align: center;
  background: rgba(255,255,255, .95);
}
.lb-overlay > div {
  position: relative;
  width: 100%;
  margin: 50px auto 0px auto;
}
.lb-overlay div h3 {
  text-align: center;
  font-size: 24px;
  width: 100%;
}
.lb-overlay a.lb-close{
  z-index: 1001;
  position: absolute;
  top: 5px;
  text-align: center;
  height: 23px;
  overflow: hidden;
  text-decoration: none;
}
.lb-overlay img{
  /* height: 100%; For Opera max-height does not seem to work */
  max-height: 100%;
  position: relative;
  opacity: 0;
  top: 30px;
  filter: alpha(opacity=0); /* internet explorer */
    -ms-filter:"progid:DXImageTransform.Microsoft.Alpha(opacity=0)"; /*IE8*/
  -webkit-transition: opacity 0.5s linear;
  -moz-transition: opacity 0.5s linear;
  -o-transition: opacity 0.5s linear;
  -ms-transition: opacity 0.5s linear;
  transition: opacity 0.5s linear;
}
.lb-prev, .lb-next{
  position: absolute;
  width: 24px;
  height: 25px;
  left: 50%;
  opacity: 0.8;
  filter: alpha(opacity=80); /* internet explorer */
    -ms-filter:"progid:DXImageTransform.Microsoft.Alpha(opacity=80)"; /*IE8*/
}
.lb-prev:hover, .lb-next:hover{
  opacity: 1;
  filter: alpha(opacity=99); /* internet explorer */
    -ms-filter:"progid:DXImageTransform.Microsoft.Alpha(opacity=99)"; /*IE8*/
}
.lb-prev{
  color: $body-color;
  margin-left: -30px;
  text-decoration: none;
}
.lb-next{
  color: $body-color;
  margin-left: 6px;
  text-decoration: none;
}
.lb-overlay:target {
  width: 100%;
  height: auto;
  bottom: 0px;
  right: 0px;
}
.lb-overlay:target img,
.lb-overlay:target a.lb-close{
  margin: 0 auto;
  opacity: 1;
  filter: alpha(opacity=99); /* internet explorer */
    -ms-filter:"progid:DXImageTransform.Microsoft.Alpha(opacity=99)"; /*IE8*/
}
/* 
  100% Height for Opera as the max-height seems to be ignored, not optimal for large screens 
  http://bricss.net/post/11230266445/css-hack-to-target-opera 
*/
x:-o-prefocus, .lb-overlay img {
    height: 100%;
}




// ==============================================================================
// #Icomoon font
// https://icomoon.io/app/
// ==============================================================================
@font-face {
  font-family: 'restoready-starter';
  src:url("{{ 'restoready-starter.eot?g9q59m' | asset_url }}");
  src:url("{{ 'restoready-starter.eot?#iefixg9q59m' | asset_url }}") format('embedded-opentype'),
    url("{{ 'restoready-starter.woff?g9q59m' | asset_url }}") format('woff'),
    url("{{ 'restoready-starter.ttf?g9q59m' | asset_url }}") format('truetype'),
    url("{{ 'restoready-starter.svg?g9q59m#restoready-starter' | asset_url }}") format('svg');
  font-weight: normal;
  font-style: normal;
}

[class^="icon-"], [class*=" icon-"] {
  font-family: 'restoready-starter';
  speak: none;
  font-style: normal;
  font-weight: normal;
  font-variant: normal;
  text-transform: none;
  line-height: 1;

  /* Better Font Rendering =========== */
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

.icon-envelope-o:before {
  content: "\f003";
}
.icon-heart:before {
  content: "\f004";
}
.icon-star:before {
  content: "\f005";
}
.icon-star-o:before {
  content: "\f006";
}
.icon-check:before {
  content: "\f00c";
}
.icon-times:before {
  content: "\f00d";
}
.icon-search-plus:before {
  content: "\f00e";
}
.icon-search-minus:before {
  content: "\f010";
}
.icon-home:before {
  content: "\f015";
}
.icon-clock-o:before {
  content: "\f017";
}
.icon-tag:before {
  content: "\f02b";
}
.icon-bookmark:before {
  content: "\f02e";
}
.icon-camera:before {
  content: "\f030";
}
.icon-photo:before {
  content: "\f03e";
}
.icon-map-marker:before {
  content: "\f041";
}
.icon-chevron-left:before {
  content: "\f053";
}
.icon-chevron-right:before {
  content: "\f054";
}
.icon-info-circle:before {
  content: "\f05a";
}
.icon-arrow-left:before {
  content: "\f060";
}
.icon-arrow-right:before {
  content: "\f061";
}
.icon-arrow-up:before {
  content: "\f062";
}
.icon-arrow-down:before {
  content: "\f063";
}
.icon-exclamation-circle:before {
  content: "\f06a";
}
.icon-warning:before {
  content: "\f071";
}
.icon-chevron-up:before {
  content: "\f077";
}
.icon-chevron-down:before {
  content: "\f078";
}
.icon-twitter-square:before {
  content: "\f081";
}
.icon-facebook-square:before {
  content: "\f082";
}
.icon-heart-o:before {
  content: "\f08a";
}
.icon-trophy:before {
  content: "\f091";
}
.icon-phone:before {
  content: "\f095";
}
.icon-phone-square:before {
  content: "\f098";
}
.icon-twitter:before {
  content: "\f099";
}
.icon-facebook:before {
  content: "\f09a";
}
.icon-navicon:before {
  content: "\f0c9";
}
.icon-google-plus-square:before {
  content: "\f0d4";
}
.icon-google-plus:before {
  content: "\f0d5";
}
.icon-envelope:before {
  content: "\f0e0";
}
.icon-envelope-square:before {
  content: "\f199";
}


// ==============================================================================
// #Breakpoints
// ==============================================================================

// Medium
@include at-query($min, $small) {
  body,
  input,
  textarea,
  select {
    font-size: 112.5%;
  }

  .logo {
    width: 17.5%;
    float: left;
    margin: 1em 1em 0 0;
  }
  .restaurant-name {
    float: left;
    text-align: left;
    padding-top: 1em;
  }
}

// Large
@include at-query($min, $medium) {
  body,
  input,
  textarea,
  select {
    font-size: 125%;
  }

  .logo {
    width: 15%;
    margin: 1em 1em 1em 0;
  }

  .phone-number {
    margin: 1.45em 0;
    text-align: right;
  }
  .main-nav__items {
    margin: 0;
    text-align: left;
  }
    .main-nav__item {
      display: inline-block;
      font-size: 1rem;
    }

  .main-nav__languages {
    text-align: center;
  }
    .main-nav__language {
      display: inline-block;
      font-size: 1rem;
      text-align: right;
    }

  /**
 * Pure CSS Lightbox
 * @see http://tympanus.net/codrops/2011/12/26/css3-lightbox/
 */
.lb-overlay a.lb-close{
  top: 45px;
}
.lb-overlay:target {
  width: auto;
  padding: 80px 100px 120px 100px;
}
}

// > Large
@include at-query($min, $siteWidth) {

}
