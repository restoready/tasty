<section class="page-header page-header--page">
  <div class="wrapper">
    <header class="section__header page-header__header">
      <h1>{{ page.title }}</h1>
    </header>
  </div>
</section>

<section class="page-body">
  <div class="wrapper">
    <main class="section__main">
      {{ page.content }}
    </main>
  </div>
</section>